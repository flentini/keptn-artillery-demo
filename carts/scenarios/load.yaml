config:
  phases:
    - duration: 120
      arrivalRate: 5
      rampTo: 100
scenarios:
  - flow:
      - get:
          url: "/"
      - get:
          url: "/carts/1"
      - get:
          url: "/carts/1/items"
      - get:
          url: "/health"
